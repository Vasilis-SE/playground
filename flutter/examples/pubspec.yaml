name: examples
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_geofence: ^0.4.1
  flutter_local_notifications: ^8.1.1+1
  geolocator: ^7.4.0
  location: ^4.3.0
  flutter_beacon: ^0.5.1
  get: ^4.3.8
  geofence_service: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/database/geofences.json
